# (이 파일은 예시입니다. 이 내용을 바탕으로 2개의 파일을 만드세요)
# 1. 'application.properties' (GCP 배포용 - Git에 올라가면 안 됨)
# 2. 'application-local.properties' (로컬 개발용 - Git에 올라가면 안 됨)


#'application.properties' 파일에 들어가야 할 내용 (GCP 배포용)


# 배포용으므로 spring.profiles.active=local 줄이 없어야 합니다.

# 1. Cloud SQL 인스턴스 연결 이름
spring.cloud.gcp.sql.instance-connection-name=[GCP_PROJECT_ID]:[REGION]:[DB_INSTANCE_NAME]

# 2. Cloud SQL DB 이름 (jdbc:mysql://google/ <- 이 부분이 중요)
spring.datasource.url=jdbc:mysql://google/[Cloud_SQL의_DB이름]?serverTimezone=UTC

# 3. Cloud SQL 계정 정보
spring.datasource.username=[Cloud_SQL_DB_ID]
spring.datasource.password=[Cloud_SQL_DB_PW]

# 4. JPA 설정 (배포용: update 또는 validate)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update


#'application-local.properties' 파일에 들어가야 할 내용 (로컬 개발용)


# 1. 로컬 MySQL 접속 정보 (키 이름이 'spring.'으로 시작해야 함)
spring.datasource.url=jdbc:mysql://localhost:3306/[내_로컬DB이름]?serverTimezone=UTC
spring.datasource.username=[내_로컬DB_ID]
spring.datasource.password=[내_로컬DB_PW]

# 2. JPA 설정 (로컬용: create 또는 create-drop)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=create